# Pinned runtime dependencies for the container image.
#
# pyproject.toml carries open ranges, which is right for a package installed
# alongside other things. This file is the application set: exact versions, so
# an image built today and one built in a year behave the same way. The
# original listed six unpinned names, which is how its build came to fail.
#
# scikit-surprise is deliberately absent. Only the legacy baseline in the
# evaluation needs it, the service does not, and it has no wheel for every
# interpreter. Install it with `pip install -e ".[legacy]"` to run that model.

numpy==2.4.6
pandas==3.0.5
scipy==1.17.1
scikit-learn==1.9.0
joblib==1.5.3
Flask==3.1.3
gunicorn==26.0.0
